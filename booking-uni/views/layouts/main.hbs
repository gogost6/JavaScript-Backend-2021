<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/static/css/style.css">
</head>

<body>

    <div id="container">
        <nav>

            {{#if user}}
            <div class="left-container">
                <ul>
                    <li><a href="/hotels">Home</a></li>
                    <li><a href="/hotels/add">Add +</a></li>
                </ul>
            </div>
            <div class="right-container">
                <a href="/profile/{{user.username}}" class="log-out">{{user.username}}</a>
                <a href="/auth/logout" class="log-out">Logout</a>
            </div>
            {{else}}
            <div class="left-container">
                <ul>
                    <li><a href="/hotels">Home</a></li>
                    <li><a href="/auth/login">Login</a></li>
                    <li><a href="/auth/register">Register</a></li>
                </ul>
            </div>
            {{/if}}
        </nav>

        {{#if errors}}
        <div class="notification errorBox">
            {{#each errors}}
            <span>{{this}}</span>
            {{/each}}
        </div>
        {{/if}}
        {{!-- <div>
            <div class="notification loadingBox">
                Loading …
            </div>
            <div class="notification infoBox">
                <span>Info</span>
            </div>
        </div> --}}


        <main>
            {{{body}}}
        </main>

        <footer>@SoftUni - JS Back-end - BookingUni 2020</footer>
    </div>

</body>

</html>